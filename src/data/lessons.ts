export type Lesson={id:string;num:string;title:string;summary:string;body:string[];code?:string;phase:1|2;interactive:'client'|'http'|'builder'|'status'|'servlet'|'spring'|'dispatcher'|'matching'|'servletReactive'|'tomcatNetty'|'blockingSim'|'perspective'|'httpClients'|'okhttp'|'webclient'|'monoFlux'|'mvcWebflux'|'requestFlow'};
export const lessons:Lesson[]=[
{id:'client-server',num:'01',title:'Client & Server',summary:'Roles in request/response communication.',phase:1,interactive:'client',body:['A client starts communication by sending a request. A server listens for requests, processes them, and returns a response.','Roles are per request: the same application can be a server in one conversation and a client in the next.','Example: Client → HTTP Request → ServiceA Service → HTTP Request → Serviceb Service. ServiceA is the server for the first request, then ServiceA becomes the client when calling Serviceb. Serviceb is the server for the second request.']},
{id:'http-fundamentals',num:'02',title:'HTTP Fundamentals',summary:'Methods, URLs, headers, parameters, bodies, responses and status codes.',phase:1,interactive:'http',code:'GET /vehicles/WDD123?region=EMEA HTTP/1.1\nHost: example.com\nAuthorization: Bearer xxx',body:['HTTP is the protocol clients and servers use to exchange requests and responses.','A URL identifies a resource. Methods like GET and POST express the action. Headers carry metadata. Query parameters follow ?. Path parameters live inside the path. A body carries payload data.','Responses include a status code, headers, and optionally a body.']},
{id:'http-request',num:'03',title:'HTTP Request',summary:'Anatomy of requests and common methods.',phase:1,interactive:'builder',body:['HTTP Request → Method, URL, Headers, Query Parameters, Path Parameters, and Body.','GET reads, POST creates/submits, PUT replaces, PATCH partially updates, and DELETE removes.','Examples: GET /vehicles/WDD123, POST /vehicles with JSON, PUT /vehicles/WDD123, PATCH /vehicles/WDD123/status, DELETE /vehicles/WDD123.']},
{id:'http-response',num:'04',title:'HTTP Response',summary:'Status codes, headers and response bodies.',phase:1,interactive:'status',body:['HTTP Response → Status Code, Headers, and Body.','Success: 200 OK, 201 Created, 202 Accepted, 204 No Content. Client errors: 400, 401, 403, 404, 409, 429. Server errors: 500, 502, 503, 504.','Choose the most specific status code that describes the outcome.']},
{id:'servlet-tomcat',num:'05',title:'Servlet & Tomcat',summary:'Servlet container basics and Java request objects.',phase:1,interactive:'servlet',code:'@GetMapping("/vehicles/{vin}")\npublic Vehicle getVehicle(\n        @PathVariable String vin,\n        HttpServletRequest request) {\n\n    String authorization =\n        request.getHeader("Authorization");\n\n    return vehicleService.getVehicle(vin);\n}',body:['A Servlet is a Java component that handles web requests. A Servlet Container runs Servlets and manages the Servlet lifecycle. Tomcat is a popular Servlet Container.','HttpServletRequest is the Java representation of an incoming HTTP request in the Servlet model. HttpServletResponse represents the outgoing response.','HTTP Request is the actual protocol-level message. HttpServletRequest is the Java object representing that request inside a Servlet-based application.']},
{id:'spring-mvc',num:'06',title:'Spring MVC',summary:'How Spring routes requests to application code.',phase:1,interactive:'spring',code:'@RestController\npublic class VehicleController {\n\n    @GetMapping("/vehicles/{vin}")\n    public Vehicle getVehicle(\n            @PathVariable String vin) {\n\n        return vehicleService.getVehicle(vin);\n    }\n}',body:['Spring MVC is a web framework built around the DispatcherServlet front controller.','Flow: Client → Tomcat → Servlet → DispatcherServlet → Handler → Controller → Service → Repository.','HandlerMapping compares GET /vehicles/WDD123 to mappings like @GetMapping("/vehicles/{vin}") and selects the matching controller method.']},
{id:'dispatcher-servlet',num:'07',title:'DispatcherServlet',summary:'The Spring MVC front controller.',phase:1,interactive:'dispatcher',body:['DispatcherServlet acts as the central/front controller in Spring MVC.','It receives the request, asks HandlerMapping which handler should run, invokes the controller method, and coordinates the response.','For @GetMapping("/vehicles/{vin}") and GET /vehicles/WDD123, the getVehicle method handles the request.']},
{id:'handler-controller',num:'08',title:'Handler & Controller',summary:'Mapping annotations and controller method parameters.',phase:1,interactive:'matching',body:['A handler is the executable code selected for a request. In Spring MVC, a controller method often acts as the handler.','@GetMapping and @PostMapping map HTTP methods and paths. @PathVariable reads /vehicles/{vin}. @RequestParam reads ?region=EMEA. @RequestBody reads the JSON body.','Controllers should coordinate HTTP concerns and delegate business logic to services.']},
{id:'servlet-vs-reactive',num:'09',title:'Servlet vs Reactive',summary:'Comparing the Servlet (Spring MVC) model with the Reactive (Spring WebFlux) model.',phase:2,interactive:'servletReactive',body:['Spring MVC is built on the Servlet model: Tomcat accepts a connection, hands it to a Servlet, and Spring’s DispatcherServlet dispatches it to a Controller and Service. This typically uses a thread-per-request model, where each request occupies a thread for the duration of its processing.','Spring WebFlux is built on a reactive model: Netty accepts the connection and requests flow through WebFlux to a Handler and Controller, typically using an event loop, where a small number of threads handle many requests by reacting to events instead of blocking and waiting.','Thread-per-request means one thread is dedicated to a request from start to finish, including while it waits on something slow. An event loop means a small number of threads continuously pick up work as it becomes ready, without being tied up waiting.','Neither model is inherently "better" — they are different trade-offs for handling concurrent requests, which matters most when a service like Order Service spends time waiting on calls to Payment Service.']},
{id:'tomcat-vs-netty',num:'10',title:'Tomcat vs Netty',summary:'The servers commonly underneath Spring MVC and Spring WebFlux.',phase:2,interactive:'tomcatNetty',body:['Tomcat is a Servlet container and is the server commonly used underneath Spring MVC applications. Netty is an asynchronous networking framework commonly used underneath Spring WebFlux applications.','Netty itself is not tied to any single framework — it is a general-purpose networking framework that WebFlux happens to build its reactive HTTP server on top of.','Reactive does not automatically mean "faster." For simple, fast operations a blocking Servlet-based application can perform just as well, or better. The main advantage of the reactive model shows up under I/O-heavy workloads with many concurrent, slow downstream calls, where non-blocking processing lets a small number of threads serve far more concurrent requests.','The application programming model (Spring MVC vs Spring WebFlux) and the underlying server (Tomcat vs Netty) are related but distinct concepts. Treat "Tomcat = blocking" and "Netty = non-blocking" as a common default, not an absolute rule.']},
{id:'blocking-vs-nonblocking',num:'11',title:'Blocking vs Non-blocking',summary:'What happens to a thread while Order Service waits on Payment Service.',phase:2,interactive:'blockingSim',body:['Consider Order Service calling Payment Service, where Payment Service takes about 2 seconds to respond.','In a blocking call, the thread handling the request calls Payment Service and then waits — it stays occupied for the full 2 seconds and cannot do anything else, including serving other requests.','In a non-blocking call, the thread that made the call is freed to do other work while waiting for Payment Service. When the response arrives, processing continues, often on a different event-loop thread rather than the original one.','Under low concurrency the difference may barely be visible. Under high concurrency, blocking calls can exhaust a fixed thread pool, causing new requests to queue up or fail, while a non-blocking design can keep accepting far more concurrent requests with the same number of threads.']},
{id:'incoming-vs-outgoing',num:'12',title:'Incoming vs Outgoing Requests',summary:'The same request can be incoming for one service and outgoing for another.',phase:2,interactive:'perspective',body:['Whether a request is "incoming" or "outgoing" depends entirely on which service you are looking from.','From the Order Service perspective, a request from a Customer is incoming — Order Service is acting as the HTTP server for that call.','From the Order Service perspective, a request it sends to Payment Service is outgoing — Order Service is acting as the HTTP client for that call.','The call Order Service makes to Payment Service is simultaneously outgoing for Order Service and incoming for Payment Service. Client and server are roles in a particular communication, not fixed labels on a service.']},
{id:'http-clients',num:'13',title:'HTTP Clients',summary:'Libraries used to make outgoing HTTP requests.',phase:2,interactive:'httpClients',body:['When Order Service needs to make an outgoing HTTP request — for example, to Payment Service — it uses an HTTP client library to build, send, and read the response for that request.','Common Java/Spring HTTP clients include OkHttp, RestTemplate, WebClient, and Feign.','These clients differ in programming model: some are blocking, some support non-blocking/reactive usage, and some are declarative. A library and a programming model are related but not identical — OkHttp, for example, is a general-purpose HTTP client that can be used both synchronously and asynchronously depending on how it is called.']},
{id:'okhttp',num:'14',title:'OkHttp',summary:'A widely used synchronous-by-default HTTP client for Java.',phase:2,interactive:'okhttp',code:'OkHttpClient client = new OkHttpClient();\n\nRequest request = new Request.Builder()\n        .url("https://payment-service/payments")\n        .post(requestBody)\n        .build();\n\ntry (Response response = client.newCall(request).execute()) {\n    // process response\n}',body:['OkHttp is a widely used HTTP client library for Java.','The Request object represents the outgoing HTTP request being sent from Order Service to Payment Service, including the URL, method and body. The Response object represents what comes back from Payment Service.','Calling client.newCall(request).execute() sends the request and blocks the calling thread until a response (or an error) is received — this is a synchronous, blocking call in this example. OkHttp also supports asynchronous calls via enqueue(), which does not block the calling thread.']},
{id:'webclient',num:'15',title:'WebClient',summary:'Spring’s reactive HTTP client for outgoing requests.',phase:2,interactive:'webclient',code:'WebClient client = WebClient.create();\n\nMono<PaymentResponse> response =\n    client.post()\n          .uri("/payments")\n          .bodyValue(paymentRequest)\n          .retrieve()\n          .bodyToMono(PaymentResponse.class);',body:['WebClient is Spring’s reactive HTTP client, typically used to make outgoing calls in a non-blocking way.','Rather than blocking the calling thread until a response arrives, WebClient returns a Mono (or Flux) immediately, representing a value that will be available later. The rest of the processing is expressed as steps in a reactive pipeline that run once the response actually arrives.','Using WebClient does not automatically make an entire application reactive or non-blocking. If the surrounding code still blocks — for example by calling .block() on the Mono, or by running inside a blocking Servlet-based controller — the non-blocking benefit can be lost.']},
{id:'mono-flux',num:'16',title:'Mono & Flux',summary:'Reactor types for representing zero-to-one and zero-to-many results.',phase:2,interactive:'monoFlux',body:['Mono and Flux are Reactor types used to represent asynchronous sequences of data in Spring WebFlux.','A Mono<Payment> represents zero or one result — for example, looking up a single Payment by id.','A Flux<Payment> represents zero to many results — for example, a stream of Payments for a customer.','Both are Publishers: nothing happens until a Subscriber subscribes. Operators like map, flatMap, and filter build a reactive pipeline that transforms data as it becomes available, without blocking the calling thread.']},
{id:'mvc-vs-webflux',num:'17',title:'Spring MVC vs WebFlux',summary:'Side-by-side comparison of the two Spring web stacks.',phase:2,interactive:'mvcWebflux',body:['Spring MVC and Spring WebFlux are two ways to build HTTP APIs in Spring, suited to different situations.','Spring MVC is built on the Servlet stack, commonly runs on Tomcat, and is typically used with blocking APIs and the traditional thread-per-request model. Its request object is HttpServletRequest.','Spring WebFlux is built on a reactive stack, commonly runs on Netty, uses Mono/Flux return types, and follows an event-loop model intended for non-blocking APIs.','The choice depends on the workload: WebFlux tends to help most when a service spends a lot of time waiting on other services, like Order Service calling Payment Service repeatedly under high concurrency. For CPU-bound or low-concurrency work, Spring MVC is often simpler and just as effective.']},
{id:'interactive-request-flow',num:'18',title:'Interactive Request Flow',summary:'Combine application model and direction to explore the full request path.',phase:2,interactive:'requestFlow',body:['This lesson combines everything from Phase 2: pick an application model (Servlet/Spring MVC or Reactive/Spring WebFlux) and a communication direction (incoming or outgoing) to see the resulting request flow.','Click any node in the diagram to see what it is, the role it plays, an example, whether it belongs to an incoming or outgoing request, and how it relates to blocking vs non-blocking processing.','Remember: the same service can appear on both sides. Order Service is the server for incoming Customer requests, and the client for its outgoing calls to Payment Service.']}
];
