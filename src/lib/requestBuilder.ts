export type Method='GET'|'POST'|'PUT'|'PATCH'|'DELETE';
export function buildHttpRequest(method:Method,path:string,query:string,header:string,body:string){const cleanPath=path.trim()||'/orders/ORD123';const q=query.trim();const h=header.trim();let out=`${method} ${cleanPath}${q?`?${q}`:''} HTTP/1.1\nHost: example.com`;if(h)out+=`\n${h}`;if(body.trim())out+=`\nContent-Type: application/json\n\n${body.trim()}`;return out}
